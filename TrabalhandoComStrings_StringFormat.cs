using System;
using System.Collections.Generic;
using System.Text;

namespace AvancandoCSharp
{
    class TrabalhandoComStrings_StringFormat
    {
        //Trabalhando Com Strings - String Format
        /*
        string nome = "Jose da Silva";
        int idade = 64;
        double rendimento = 4000;
        DateTime dataCadastro = new DateTime(2014, 03, 31);

        string str = "O cliente {0} tem {1} anos de idade e renda mensal de {2:c}.";
        str += " e é cliente desde {3:dd/MM/yyyy}.";
            string frase = String.Format(str, nome, idade, rendimento, dataCadastro);
        WriteLine(frase);
        */
    }
}
